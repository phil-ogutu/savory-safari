import React from "react";

const FavouriteButton = ({ postId }) => {
  return (
    <button className="text-yellow-500 hover:text-yellow-600">⭐ Save</button>
  );
};

export default FavouriteButton;
